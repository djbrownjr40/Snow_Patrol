<%# if user exists redirect to a page statement here (else use code below) %>
<div class="container">
  <div class="mt-5 mb-2 text-center">
    <h1>Welcome to Snow Patrol!</h1>
    <p>Search for slopes with perfect snow.</p>
  </div>
  <div action-controller="map-search">
    <div action-target="search-bar">
    </div>
    <div action-target="display">
      <%= render 'shared/map' %>
    </div>
    <div action-target="submit" class="d-grid gap-2 col-8 mx-auto">
      <%= link_to 'Find Ski Resorts', "#", class: 'btn btn-primary', type: 'button', id: 'search'%>
    </div>
  </div>
